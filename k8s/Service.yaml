apiVersion: v1
kind: Service
metadata:
  name: calculator-service
  labels:
    app: calculator-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 5000
  selector:
    app: calculator-app
